@model IEnumerable<VacationEnvyFE5.Models.ProfileViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Profile/Index.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FromUser.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ToUser.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FromUser.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ToUser.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserFollowId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserFollowId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserFollowId })
        </td>
    </tr>
}

</table>
